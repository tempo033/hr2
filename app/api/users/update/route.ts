import { NextRequest, NextResponse } from 'next/server'
import { getServerAuth, supabaseHeaders } from '@/lib/server-auth'
const SUPABASE_URL=process.env.NEXT_PUBLIC_SUPABASE_URL||'https://pdkdvaisggntdrvpxuur.supabase.co'
const BOOTSTRAP_EMAIL='hr@albenyah.sa'
const ROLES=new Set(['admin','hr','interviewer','manager'])
export async function PATCH(req:NextRequest){try{const auth=await getServerAuth(req,['admin']);if(!auth)return NextResponse.json({error:'لا تملك صلاحية تعديل المستخدمين.'},{status:403});const body=await req.json();if(!body.user_id)return NextResponse.json({error:'معرف المستخدم مطلوب.'},{status:400});if(body.role!==undefined&&!ROLES.has(body.role))return NextResponse.json({error:'الصلاحية المحددة غير صحيحة.'},{status:400});if(body.is_active!==undefined&&typeof body.is_active!=='boolean')return NextResponse.json({error:'حالة المستخدم غير صحيحة.'},{status:400});if(body.role===undefined&&body.is_active===undefined)return NextResponse.json({error:'لا توجد تغييرات.'},{status:400});if(body.user_id===auth.user.id&&body.is_active===false)return NextResponse.json({error:'لا يمكن إيقاف حسابك الحالي.'},{status:400});if(body.user_id===auth.user.id&&body.role&&body.role!=='admin')return NextResponse.json({error:'لا يمكن إزالة صلاحية مدير النظام من حسابك الحالي.'},{status:400});const headers=supabaseHeaders(auth);if(body.is_active===false&&auth.serviceKey){const target=await fetch(`${SUPABASE_URL}/auth/v1/admin/users/${body.user_id}`,{headers:{apikey:auth.serviceKey,Authorization:`Bearer ${auth.serviceKey}`},cache:'no-store'});if(!target.ok)return NextResponse.json({error:'تعذر العثور على المستخدم.'},{status:404});const t=await target.json();if(t.email?.toLowerCase()===BOOTSTRAP_EMAIL)return NextResponse.json({error:'حساب الإدارة الأساسي لا يمكن إيقافه.'},{status:400})}const payload:any={};if(body.role!==undefined)payload.role=body.role;if(body.is_active!==undefined)payload.is_active=body.is_active;const update=await fetch(`${SUPABASE_URL}/rest/v1/app_users?user_id=eq.${body.user_id}`,{method:'PATCH',headers:{...headers,Prefer:'return=representation'},body:JSON.stringify(payload),cache:'no-store'});if(!update.ok){const text=await update.text();return NextResponse.json({error:text||'تعذر تحديث المستخدم.'},{status:500})}const rows=await update.json();if(!rows?.length)return NextResponse.json({error:'المستخدم غير موجود في ملف الصلاحيات.'},{status:404});return NextResponse.json({user:rows[0]})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'حدث خطأ غير متوقع.'},{status:500})}}

import {NextRequest,NextResponse} from 'next/server'
import { getServerAuth, supabaseHeaders } from '@/lib/server-auth'
const URL=process.env.NEXT_PUBLIC_SUPABASE_URL||'https://pdkdvaisggntdrvpxuur.supabase.co'
const ROLES=['admin','hr','interviewer','manager','interview_viewer']
async function get(path:string,a:any){const r=await fetch(`${URL}/rest/v1/${path}`,{headers:supabaseHeaders(a),cache:'no-store'});if(!r.ok)throw new Error(await r.text());return r.json()}
export async function GET(req:NextRequest){try{const a=await getServerAuth(req,ROLES);if(!a)return NextResponse.json({error:'غير مصرح'},{status:401});const[interviews,onboarding]=await Promise.all([get('candidate_interviews?select=candidate_id&order=created_at.desc',a),get('employee_onboarding?select=candidate_id&order=created_at.desc',a)]);const ids=Array.from(new Set([...interviews.map((x:any)=>x.candidate_id),...onboarding.map((x:any)=>x.candidate_id)]));const candidates=ids.length?await get(`candidates?select=id,full_name,request_id&id=in.(${ids.join(',')})`,a):[];const map=new Map(candidates.map((x:any)=>[x.id,x]));return NextResponse.json({candidates:interviews.map((x:any)=>map.get(x.candidate_id)).filter(Boolean),onboarding:onboarding.map((x:any)=>map.get(x.candidate_id)).filter(Boolean)},{headers:{'Cache-Control':'no-store'}})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'تعذر تحميل التقارير'},{status:500})}}

'use client'

import { useEffect, useMemo, useState } from 'react'
import { supabase } from '@/lib/supabase'

type CandidatePageProps = { params: Promise<{ token: string }> }
type Score = { score: number; evidence: string }
type Requirement = { id: string; name: string; category?: string; required?: boolean }
type Option = { label: string; score: number }

const getRequirementOptions = (r: Requirement): Option[] => {
  const name = String(r.name || '').toLowerCase()
  const category = String(r.category || '')
  if (category === 'اعتماد' || /اعتماد|شهادة|عضوية|رخصة|تصنيف/.test(name)) return [{label:'غير متوفر',score:0},{label:'متوفر',score:100}]
  if (/autocad|revit|bim|excel|primavera|ms project|برنامج|software|sap|erp/.test(name)) return [{label:'لا يستخدم',score:0},{label:'أساسي',score:25},{label:'جيد',score:50},{label:'متقدم',score:75},{label:'متقن',score:100}]
  if (/خبرة|experience|سنوات/.test(name)) return [{label:'لا توجد خبرة',score:0},{label:'أقل من سنة',score:25},{label:'1–3 سنوات',score:50},{label:'3–5 سنوات',score:75},{label:'أكثر من 5 سنوات',score:100}]
  if (/قراءة|فهم|حصر|quantity|كميات|boq|مستخلص|shop|as-built|material|rfi|رسومات|مخططات/.test(name)) return [{label:'لا يجيد',score:0},{label:'أساسيات',score:25},{label:'جيد',score:50},{label:'متقدم',score:75},{label:'متمكن',score:100}]
  if (/لغة|english|إنجليزي|عربي/.test(name)) return [{label:'لا يجيد',score:0},{label:'محدود',score:25},{label:'متوسط',score:50},{label:'جيد',score:75},{label:'ممتاز',score:100}]
  if (/رخصة|قيادة|سيارة|نقل|إقامة|كفالة|مباشرة|متاح|جاهز/.test(name)) return [{label:'غير متوفر',score:0},{label:'غير واضح',score:25},{label:'متوفر بشروط',score:50},{label:'متوفر',score:75},{label:'متوفر دون عائق',score:100}]
  return [{label:'لا توجد خبرة',score:0},{label:'محدودة',score:25},{label:'مناسبة',score:50},{label:'جيدة',score:75},{label:'متقدمة',score:100}]
}

export default function CandidatePage({ params }: CandidatePageProps) {
  const [token,setToken]=useState(''),[loading,setLoading]=useState(true),[sent,setSent]=useState(false),[error,setError]=useState('')
  const [request,setRequest]=useState<any>(null),[candidate,setCandidate]=useState<any>(null),[requirements,setRequirements]=useState<Requirement[]>([])
  const [scores,setScores]=useState<Record<string,Score>>({})
  const [form,setForm]=useState({full_name:'',phone:'',email:'',city:'',nationality:'',degree:'',specialization:'',university:'',graduation_year:'',total_experience_years:'',saudi_experience_years:'',previous_experience:'',notes:''})
  useEffect(()=>{let cancelled=false;const load=async()=>{const p=await params;if(cancelled)return;setToken(p.token);if(!supabase){setError('لم يتم إعداد الاتصال بقاعدة البيانات بعد.');setLoading(false);return};const {data,error:e}=await supabase.rpc('get_candidate_public_form',{p_token:p.token});if(e||!data?.ok){setError(data?.message||'رابط المرشح غير صحيح أو غير متاح.');setLoading(false);return};const c=data.candidate;setCandidate(c);setRequest(data.request);setRequirements(data.requirements||[]);setForm({full_name:c.full_name||'',phone:c.phone||'',email:c.email||'',city:c.city||'',nationality:c.nationality||'',degree:c.degree||'',specialization:c.specialization||'',university:c.university||'',graduation_year:c.graduation_year?.toString()||'',total_experience_years:c.total_experience_years?.toString()||'',saudi_experience_years:c.saudi_experience_years?.toString()||'',previous_experience:c.previous_experience||'',notes:c.notes||''});const m:Record<string,Score>={};(data.scores||[]).forEach((x:any)=>m[x.requirement_id]={score:x.score,evidence:x.evidence||''});setScores(m);setLoading(false)};load();return()=>{cancelled=true}},[params])
  const grouped=useMemo(()=>requirements.reduce((a:any,r:any)=>{(a[r.category||'متطلبات الوظيفة'] ||= []).push(r);return a},{}),[requirements])
  const setRequirement=(id:string,score:number,evidence=scores[id]?.evidence||'')=>setScores({...scores,[id]:{score,evidence}})
  const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');if(!supabase||!candidate||!token)return;if(!form.full_name||!form.phone){setError('يرجى إدخال الاسم الكامل ورقم الجوال.');return};const {data,error:e1}=await supabase.rpc('save_candidate_public_form',{p_token:token,p_candidate:form,p_scores:requirements.map(r=>({requirement_id:r.id,score:scores[r.id]?.score??0,evidence:scores[r.id]?.evidence||''}))});if(e1||!data?.ok){setError(data?.message||'تعذر حفظ البيانات. حاول مرة أخرى.');return}setSent(true)}
  if(loading)return <main className="min-h-screen grid place-items-center">جاري تحميل نموذج المرشح...</main>
  if(error&&!request)return <main className="min-h-screen grid place-items-center p-6"><div className="card p-8 text-center text-red-600">{error}</div></main>
  return <main className="min-h-screen bg-slate-50 py-8 px-4" dir="rtl"><div className="max-w-4xl mx-auto"><div className="bg-[#09233f] text-white rounded-2xl p-6 mb-6"><div className="text-[#d3a62a] font-bold">البنية الاساسية للمقاولات</div><h1 className="text-2xl font-bold mt-2">نموذج بيانات المرشح</h1><p className="text-slate-300 mt-2">الوظيفة: {request?.exact_type}</p><p className="text-slate-300 text-sm mt-1">هذا الرابط مخصص لبياناتك فقط ولا يتيح الوصول إلى صفحات أو بيانات منظومة الموارد البشرية.</p></div>
  {sent?<div className="card p-10 text-center"><div className="text-5xl mb-4">✓</div><h2 className="text-2xl font-bold text-[#09233f]">تم إرسال بياناتك بنجاح</h2><p className="text-slate-500 mt-3">شكرًا لك. ستتم مراجعة بياناتك من قبل فريق الموارد البشرية.</p></div>:<form onSubmit={submit} className="space-y-6"><section className="card p-6"><h2 className="section-title">البيانات الشخصية</h2><div className="grid md:grid-cols-2 gap-4"><input required className="input" placeholder="الاسم الكامل *" value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/><input required className="input" placeholder="رقم الجوال *" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><input className="input" placeholder="البريد الإلكتروني" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="input" placeholder="المدينة" value={form.city} onChange={e=>setForm({...form,city:e.target.value})}/><input className="input" placeholder="الجنسية" value={form.nationality} onChange={e=>setForm({...form,nationality:e.target.value})}/></div></section><section className="card p-6"><h2 className="section-title">المؤهلات والخبرة</h2><div className="grid md:grid-cols-2 gap-4"><input className="input" placeholder="المؤهل" value={form.degree} onChange={e=>setForm({...form,degree:e.target.value})}/><input className="input" placeholder="التخصص" value={form.specialization} onChange={e=>setForm({...form,specialization:e.target.value})}/><input className="input" placeholder="الجامعة" value={form.university} onChange={e=>setForm({...form,university:e.target.value})}/><input className="input" type="number" placeholder="سنة التخرج" value={form.graduation_year} onChange={e=>setForm({...form,graduation_year:e.target.value})}/><input className="input" type="number" step="0.5" placeholder="إجمالي سنوات الخبرة" value={form.total_experience_years} onChange={e=>setForm({...form,total_experience_years:e.target.value})}/><input className="input" type="number" step="0.5" placeholder="سنوات الخبرة داخل السعودية" value={form.saudi_experience_years} onChange={e=>setForm({...form,saudi_experience_years:e.target.value})}/></div><textarea className="input mt-4 min-h-28" placeholder="تفاصيل الخبرات السابقة والشركات والمشاريع" value={form.previous_experience} onChange={e=>setForm({...form,previous_experience:e.target.value})}/></section><section className="card p-6"><h2 className="section-title">المهارات والمتطلبات</h2><p className="text-sm text-slate-500 mb-5">تم تخصيص الاختيارات لكل سؤال حسب طبيعة المتطلب، وليست قائمة موحدة لجميع الأسئلة.</p>{Object.entries(grouped).map(([cat,rs]:any)=><div key={cat} className="mb-7"><h3 className="font-bold text-[#09233f] border-b pb-2 mb-3">{cat}</h3>{rs.map((r:Requirement)=><div key={r.id} className="border rounded-xl p-4 mb-3"><div className="font-medium">{r.name} {r.required&&<span className="text-red-600 text-xs">• أساسي</span>}</div><div className="grid md:grid-cols-2 gap-3 mt-3"><select className="input" value={scores[r.id]?.score??0} onChange={e=>setRequirement(r.id,Number(e.target.value))}>{getRequirementOptions(r).map(o=><option key={`${r.id}-${o.score}`} value={o.score}>{o.label}</option>)}</select><input className="input" placeholder="تفاصيل اخري" value={scores[r.id]?.evidence||''} onChange={e=>setRequirement(r.id,scores[r.id]?.score??0,e.target.value)}/></div></div>)}</div>)}</section><section className="card p-6"><h2 className="section-title">ملاحظات إضافية</h2><textarea className="input min-h-28" placeholder="أي معلومات إضافية" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></section>{error&&<div className="bg-red-50 text-red-700 rounded-xl p-4">{error}</div>}<button className="btn-gold w-full py-4 rounded-xl font-bold text-lg" type="submit">إرسال بيانات المرشح</button></form>}</div></main>
}

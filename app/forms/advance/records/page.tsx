'use client'
import {useEffect,useState} from 'react'
import Link from 'next/link'
import {ArrowLeft,Copy,ExternalLink,RefreshCw,CheckCircle2,Clock3} from 'lucide-react'
type Row={id:string;employee_name:string|null;employee_number:string|null;department:string|null;status:string;updated_at:string;last_ip_address:string|null;last_device_name:string|null;submitted_via_link:boolean;employee_id?:string|null}
export default function Records(){const [rows,setRows]=useState<Row[]>([]);const [loading,setLoading]=useState(true);const [link,setLink]=useState<any>(null);const [employees,setEmployees]=useState<any[]>([]);const [employeeId,setEmployeeId]=useState('');const [selected,setSelected]=useState<Row|null>(null);const [links,setLinks]=useState<any[]>([])
 const load=async()=>{setLoading(true);const [r,l,e]=await Promise.all([fetch('/api/forms/records?form_type=advance',{cache:'no-store'}),fetch('/api/forms/links',{cache:'no-store'}),fetch('/api/employees/data',{cache:'no-store'})]);const d=await r.json().catch(()=>({}));const ld=await l.json().catch(()=>({}));const ed=await e.json().catch(()=>({}));setRows(d.records||[]);setLink((ld.links||[]).find((x:any)=>x.form_type==='advance'&&!x.record_id)||null);setEmployees(ed.employees||ed.records||[]);setLinks(ld.links||[]);setLoading(false)};useEffect(()=>{load()},[])
 const make=async()=>{if(!employeeId)return;const r=await fetch('/api/forms/links',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({form_type:'advance',employee_id:employeeId})});const d=await r.json();if(r.ok){setLink(d.link);await navigator.clipboard?.writeText(location.origin+'/forms/public/'+d.link.token);await load()}}
 return <main dir="rtl" className="min-h-screen bg-[#f5f7fa] p-5 md:p-8"><div className="max-w-6xl mx-auto"><header className="flex justify-between items-center mb-6"><div><div className="text-[#b88618] font-bold">مركز النماذج / طلبات السلف المالية</div><h1 className="text-3xl font-black text-[#09233f]">طلبات السلف المالية</h1><p className="text-slate-500 mt-1">جميع النماذج المرسلة وحالات الوصول وبيانات الجهاز وIP.</p></div><div className="flex gap-2"><Link href="/forms" className="border bg-white rounded-xl px-4 py-2 font-bold inline-flex gap-2 items-center"><ArrowLeft size={16}/> مركز النماذج</Link><button onClick={load} className="bg-[#09233f] text-white rounded-xl px-4 py-2 font-bold inline-flex gap-2"><RefreshCw size={16}/> تحديث</button></div></header>
 <section className="bg-[#09233f] text-white rounded-2xl p-5 mb-6"><h2 className="font-black text-lg">إنشاء طلب سلفة ورابط الموظف</h2><p className="text-slate-300 text-sm mt-1">اختر الموظف، وسيتم تحميل جميع بياناته من ملف الموظف داخل نموذج السلفة.</p><div className="mt-4 flex flex-wrap gap-2"><select value={employeeId} onChange={e=>setEmployeeId(e.target.value)} className="flex-1 min-w-[280px] rounded-lg px-3 py-2 text-slate-800"><option value="">اختر الموظف</option>{employees.map((e:any)=><option key={e.id} value={e.id}>{e.employee_number||'بدون رقم'} — {e.full_name||'بدون اسم'}</option>)}</select>{link?<><input readOnly value={location.origin+'/forms/public/'+link.token} dir="ltr" className="flex-1 min-w-[260px] rounded-lg px-3 py-2 text-slate-800"/><button onClick={()=>navigator.clipboard?.writeText(location.origin+'/forms/public/'+link.token)} className="bg-[#b88618] rounded-lg px-4 py-2 font-bold inline-flex gap-1"><Copy size={16}/> نسخ</button><a target="_blank" rel="noreferrer" href={location.origin+'/forms/public/'+link.token} className="bg-white text-[#09233f] rounded-lg px-4 py-2 font-bold inline-flex gap-1"><ExternalLink size={16}/> فتح</a></>:<button disabled={!employeeId} onClick={make} className="bg-[#b88618] rounded-lg px-4 py-2 font-bold disabled:opacity-50">إنشاء رابط طلب السلفة</button>}</div></section>
 <section className="bg-white border rounded-2xl overflow-hidden"><div className="p-4 border-b font-black">سجل طلبات السلف المالية</div>{loading?<div className="p-10 text-center">جارٍ التحميل...</div>:<div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-100"><tr><th className="p-3 text-right">الموظف</th><th className="p-3 text-right">القسم</th><th className="p-3 text-right">الحالة</th><th className="p-3 text-right">الجهاز</th><th className="p-3 text-right">IP</th><th className="p-3 text-right">التاريخ</th><th className="p-3 text-right">الإجراءات</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t"><td className="p-3 font-bold">{r.employee_name||'—'}<div className="text-xs text-slate-500">{r.employee_number||''}</div></td><td className="p-3">{r.department||'—'}</td><td className="p-3">{r.status}</td><td className="p-3">{r.submitted_via_link?r.last_device_name||'غير معروف':'داخلي'}</td><td className="p-3 font-mono text-xs">{r.submitted_via_link?r.last_ip_address||'—':'—'}</td><td className="p-3">{new Date(r.updated_at).toLocaleString('ar-SA')}</td><td className="p-3"><button onClick={()=>setSelected(r)} className="bg-emerald-700 text-white rounded-lg px-3 py-1.5 font-bold ml-1">روابط الاعتماد</button><Link href={`/forms/records/${r.id}` className="bg-[#09233f] text-white rounded-lg px-3 py-1.5 font-bold">عرض / تعديل</Link></td></tr>)}</tbody></table>{!rows.length&&<div className="p-8 text-center text-slate-500">لا توجد نماذج مسجلة.</div>}</div>}</section>{selected&&<section className="mt-6 bg-white border rounded-2xl p-5"><div className="flex justify-between items-center mb-4"><div><h2 className="text-xl font-black text-[#09233f]">روابط اعتماد طلب السلفة</h2><p className="text-sm text-slate-500">{selected.employee_name||'—'} — جميع الروابط مرتبطة بهذا الطلب.</p></div><button onClick={()=>setSelected(null)} className="border rounded-lg px-3 py-1 font-bold">إغلاق</button></div><div className="grid md:grid-cols-3 gap-3">{links.filter((x:any)=>x.form_type==='advance'&&x.record_id===selected.id&&x.link_scope?.startsWith('advance:')).map((x:any)=><div key={x.id} className="border rounded-xl p-4"><b>{x.link_scope==='advance:direct_manager'?'المدير المباشر':x.link_scope==='advance:finance'?'الإدارة المالية':'الموارد البشرية'}</b><div className={x.last_submitted_at?'text-emerald-700':'text-amber-700'}>{x.last_submitted_at?<><CheckCircle2 size={15} className="inline ml-1"/>تم الاعتماد</>:<><Clock3 size={15} className="inline ml-1"/>قيد الانتظار</>}</div><div className="flex gap-2 mt-3"><a target="_blank" rel="noreferrer" href={location.origin+'/forms/public/'+x.token} className="bg-[#09233f] text-white rounded-lg px-3 py-1.5 font-bold">فتح الرابط</a><button onClick={()=>navigator.clipboard?.writeText(location.origin+'/forms/public/'+x.token)} className="border rounded-lg px-3 py-1.5 font-bold">نسخ</button></div></div>)}</div></section>}</div></main>}

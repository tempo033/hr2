'use client'

import Link from 'next/link'
import { useEffect, useState } from 'react'
import { ArrowRight, Printer, ClipboardCheck } from 'lucide-react'
import { useParams } from 'next/navigation'
import { supabase } from '@/lib/supabase'

type Candidate = { id: string; request_id: string; full_name: string; phone: string | null; specialization: string | null; degree: string | null; total_experience_years: number | null; saudi_experience_years: number | null }
type Request = { exact_type: string | null; request_type: string | null }
type Interview = { interview_date: string | null; interview_type: string; engineering_score: number; technical_office_score: number; hr_score: number; final_score: number; recommendation: string; final_decision: string; engineering_notes: string; technical_office_notes: string; hr_notes: string; general_notes: string; created_at: string }

export default function FinalInterviewReport(){
 const {candidateId}=useParams<{candidateId:string}>(); const [candidate,setCandidate]=useState<Candidate|null>(null); const [request,setRequest]=useState<Request|null>(null); const [interview,setInterview]=useState<Interview|null>(null); const [loading,setLoading]=useState(true)
 useEffect(()=>{(async()=>{try{const r=await fetch('/api/reports/candidate/'+candidateId,{cache:'no-store'}); const b=await r.json(); if(!r.ok) throw new Error(b.error||'تعذر تحميل التقرير'); setCandidate(b.candidate||null); setInterview(b.interview||null); setRequest(b.request||null)}catch{setCandidate(null)}finally{setLoading(false)}})()},[candidateId])
 if(loading)return <main className="min-h-screen grid place-items-center" dir="rtl">جاري إعداد التقرير...</main>
 if(!candidate)return <main className="min-h-screen grid place-items-center" dir="rtl">المرشح غير موجود.</main>
 const decision=interview?.final_decision||'لم يتم اتخاذ القرار'; const score=Number(interview?.final_score||0)
 return <main dir="rtl" className="min-h-screen bg-[#f5f7fa] p-5 md:p-10"><div className="max-w-5xl mx-auto"><div className="no-print flex justify-between items-center mb-5"><Link href="/interviews" className="font-bold text-[#09233f] flex items-center gap-2"><ArrowRight size={18}/> العودة للمقابلات</Link><button onClick={()=>window.print()} className="btn-primary rounded-xl px-5 py-3 font-bold flex items-center gap-2"><Printer size={18}/> طباعة التقرير</button></div><article className="bg-white shadow-sm rounded-2xl p-7 md:p-10 print:shadow-none print:rounded-none"><header className="border-b-2 border-[#d4a72c] pb-5 flex justify-between gap-5"><div><div className="text-[#b88618] font-bold">البنية الاساسية للمقاولات</div><h1 className="text-3xl font-black text-[#09233f] mt-2">تقرير المقابلة والتقييم النهائي</h1><p className="text-slate-500 mt-1">{request?.exact_type||request?.request_type||'طلب توظيف'}</p></div><ClipboardCheck className="text-[#b88618]" size={42}/></header><section className="grid md:grid-cols-2 gap-4 mt-7"><div><span className="text-slate-500 text-sm">اسم المرشح</span><h2 className="text-2xl font-black text-[#09233f]">{candidate.full_name}</h2></div><div><span className="text-slate-500 text-sm">التخصص</span><h2 className="font-bold">{candidate.specialization||'—'}</h2></div><div><span className="text-slate-500 text-sm">الجوال</span><h2 className="font-bold">{candidate.phone||'—'}</h2></div><div><span className="text-slate-500 text-sm">المؤهل</span><h2 className="font-bold">{candidate.degree||'—'}</h2></div></section><section className="grid md:grid-cols-3 gap-4 mt-7"><div className="border rounded-xl p-5"><div className="text-sm text-slate-500">الإدارة الهندسية — 40%</div><div className="text-3xl font-black text-[#09233f]">{Number(interview?.engineering_score||0)}%</div></div><div className="border rounded-xl p-5"><div className="text-sm text-slate-500">المكتب الفني — 35%</div><div className="text-3xl font-black text-[#09233f]">{Number(interview?.technical_office_score||0)}%</div></div><div className="border rounded-xl p-5"><div className="text-sm text-slate-500">الموارد البشرية — 25%</div><div className="text-3xl font-black text-[#09233f]">{Number(interview?.hr_score||0)}%</div></div></section><section className="mt-7 rounded-2xl bg-[#09233f] text-white p-7 flex justify-between items-center"><div><div className="text-sm opacity-80">النتيجة النهائية الموزونة</div><div className="text-5xl font-black mt-1">{score}%</div></div><div className="text-left"><div className="text-sm opacity-80">القرار النهائي</div><div className="text-2xl font-black mt-1">{decision}</div><div className="mt-2">{interview?.recommendation||'—'}</div></div></section><section className="grid md:grid-cols-3 gap-5 mt-7"><div><h3 className="font-black text-[#09233f] mb-2">ملاحظات الإدارة الهندسية</h3><p className="whitespace-pre-wrap text-slate-600">{interview?.engineering_notes||'لا توجد ملاحظات.'}</p></div><div><h3 className="font-black text-[#09233f] mb-2">ملاحظات المكتب الفني</h3><p className="whitespace-pre-wrap text-slate-600">{interview?.technical_office_notes||'لا توجد ملاحظات.'}</p></div><div><h3 className="font-black text-[#09233f] mb-2">ملاحظات الموارد البشرية</h3><p className="whitespace-pre-wrap text-slate-600">{interview?.hr_notes||'لا توجد ملاحظات.'}</p></div></section><section className="mt-7 border-t pt-5"><h3 className="font-black text-[#09233f]">الملاحظات العامة</h3><p className="whitespace-pre-wrap text-slate-600 mt-2">{interview?.general_notes||'لا توجد ملاحظات.'}</p></section><footer className="grid md:grid-cols-3 gap-8 mt-12 pt-8 border-t text-center"><div>الإدارة الهندسية<br/><br/>________________</div><div>إدارة المكتب الفني<br/><br/>________________</div><div>إدارة الموارد البشرية<br/><br/>________________</div></footer></article></div></main>
}